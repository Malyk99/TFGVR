<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppFirebaseLittleDemons"
        tools:targetApi="31">
        <activity
            android:name=".activities.FirstScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Main Screen Activity -->
        <activity android:name=".activities.MainActivity"/>

        <!-- Room Selection Activity -->
        <activity android:name=".activities.RoomSelectionActivity" />

        <!-- Manual Entry Activity -->
        <activity android:name=".activities.ManualEntryActivity" />

        <!-- Waiting Lobby Activity -->
        <activity android:name=".activities.WaitingLobbyActivity" />

        <!-- Game Selection Screen -->
        <activity
            android:name=".activities.GameSelectActivity"
            android:screenOrientation="landscape"/>

        <!-- Minigame 1 -->
        <activity android:name=".activities.Minigame1Activity"
            android:screenOrientation="landscape" />

        <!-- Minigame 2 -->
        <activity android:name=".activities.Minigame2Activity"
            android:screenOrientation="landscape" />

        <!-- Minigame 3 -->
        <activity android:name=".activities.Minigame3Activity"
            android:screenOrientation="landscape" />

        <!-- Minigame 4 -->
        <activity android:name=".activities.Minigame4Activity"
            android:screenOrientation="landscape" />


        <!-- Admin View -->
        <activity android:name=".activities.AdminViewActivity" />
        <activity android:name=".activities.AdminRoomPlayersActivity" />







    </application>

</manifest>